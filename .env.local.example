# LLM 后端按优先级自动判别。优先级：
#   1. 本机 claude CLI 可用             → 走本机订阅（最适合本地开发）
#   2. OPENROUTER_API_KEY 真实 key     → 走 OpenRouter 免费模型（部署 / GitHub 拉代码必备）
#   3. 都没有                           → 启动报错，提示用户去 /settings 自带 key（BYOK）
#
# BYOK（自带 key）独立于以上 3 档：用户在浏览器 /settings 粘自己的 OpenRouter key 即可，
# 服务端零配置也能跑。详见 README.md。
#
# 想强制指定（绕过自动判别，用来本机测 OpenRouter 表现），把下行取消注释：
# LLM_PROVIDER=openrouter     # cli | openrouter | auto（默认 auto = 自动判别）

# ===== 模式 A：本机 claude CLI =====
# 无 env 配置即可使用（先 `claude /login`）。
# CLI 路径用的 Claude 模型名：默认 sonnet 兜底，Step 3 诊断在 lib/prompts.ts 单独配置 opus。
ANTHROPIC_MODEL=claude-sonnet-4-6

# ===== 模式 B：OpenRouter 免费模型（无 Claude 账号也能跑）=====
# 1. 去 https://openrouter.ai 注册账号，免费 key 在 dashboard 拿
# 2. 主力 model 默认 openai/gpt-oss-120b:free；备用链：openrouter/free → nvidia/nemotron-...
# 3. 输出稳定性略低于 Claude，已在 lib/claude.ts 启用 JSON 容错重试 + 反捏造硬规则强化
OPENROUTER_API_KEY=sk-or-v1-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
OPENROUTER_MODEL=openai/gpt-oss-120b:free
